<h1>{{title}}</h1>
<h5><strong>Refugee, migrant and GDP data comparison</strong></h5>
<form>
    <div class="row">
        <div class="six columns">
            <label for="fromCountryDropdown">From</label>
            <select id="fromCountryDropdown" name="fromCountryDropdown" class="form-control">
                {{#countries}}
                <option value="{{this._id}}">{{this.name}}</option>
                {{/countries}}
            </select>
        </div>

        <div class="six columns">
            <label for="toCountryDropdown">To</label>
            <select id="toCountryDropdown" name="toCountryDropdown" class="form-control">
                {{#countries}}
                <option value="{{this._id}}">{{this.name}}</option>
                {{/countries}}
            </select>
        </div>

    </div>

    <div class="row">
        <div class="chart-container">
            <canvas id="chart" width="900" height="300"></canvas>
        </div>
    </div>

    <div class="row">
        <div class="table-container">
            <table class="table u-full-width">
                <thead>
                    <tr>
                        <th>Year</th>
                        <th>Refugees</th>
                        <th>Total migrants</th>
                        <th>∆GDP %</th>
                    </tr>
                </thead>
                <tbody id="migration-table-body">
                    <tr class="migration-table-entry">
                        <td> </td>
                        <td> </td>
                        <td> </td>
                        <td> </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
